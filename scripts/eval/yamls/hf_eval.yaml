max_seq_len: 1024
seed: 1
precision: fp32

# If you are using one model, put it here:
model_name_or_path: "/leonardo_scratch/large/userexternal/gpuccett/models/hf_gpt_neo/gpt-neo-125m"
# otherwise, write a block for each model you want to test in the `models` section

models:
-
  model_name: "gpt-neo-125m"
  model:
    name: hf_causal_lm
    pretrained_model_name_or_path: ${model_name_or_path}
    init_device: cpu
    pretrained: true
  tokenizer:
    name: ${model_name_or_path}
    kwargs:
      model_max_length: ${max_seq_len}
  results_output_path: "results/gpt-neo-125m.md"
  data_output_path: "outputs/gpt-neo-125m.jsonl"

# # if you are evaluating more than one model, list them all as YAML blocks without variable interpolation
# -
#   model_name: mosaicml/mpt-7b
#   model:
#     name: hf_causal_lm
#     pretrained_model_name_or_path: mosaicml/mpt-7b
#     init_device: cpu
#     pretrained: true
#     config_overrides:
#       max_seq_len: ${max_seq_len}
#   tokenizer:
#     name: mosaicml/mpt-7b
#     kwargs:
#       model_max_length: ${max_seq_len}

device_eval_batch_size: 4

# FSDP config for model sharding
# fsdp_config:
#   sharding_strategy: FULL_SHARD
#   mixed_precision: FULL
#   forward_prefetch: True
#   limit_all_gathers: True

icl_tasks: '/leonardo_work/IscrC_GELATINO/gpuccett/Repos/llm-foundry_gpucce/scripts/eval/yamls/tasks_light.yaml'
eval_gauntlet: '/leonardo_work/IscrC_GELATINO/gpuccett/Repos/llm-foundry_gpucce/scripts/eval/yamls/eval_gauntlet.yaml'
